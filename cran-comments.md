## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new submission.

## Test environments

* local macOS (aarch64-apple-darwin20), R 4.4.0
* GitHub Actions (ubuntu-latest), R release
* GitHub Actions (windows-latest), R release
* R-hub (linux, windows, macos)

## Notes

The NOTE about "unable to verify current time" is a network connectivity 
issue during check and does not affect package functionality.

## Downstream dependencies

This is a new package with no downstream dependencies.
